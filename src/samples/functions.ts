function greetings(firstName: string, lastName: string): void {
    console.log(`Hello ${firstName} ${lastName}`)
}

function anotherGreetings(firstName: string, lastName: string): string {
    return `Hello ${firstName} ${lastName}`
}
